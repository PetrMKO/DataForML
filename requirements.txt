# Core — обязательные зависимости
streamlit>=1.35
pandas>=2.0
numpy>=1.24
pyarrow>=12.0
pyyaml>=6.0
requests>=2.28
beautifulsoup4>=4.12
tqdm>=4.65
scikit-learn>=1.3
imbalanced-learn>=0.11
scipy>=1.11
matplotlib>=3.7
seaborn>=0.12
tabulate>=0.9
datasets>=2.0
prefect>=2.0

# Optional — только для NER (шаг 3 с modality=text, task=ner)
# spacy>=3.7
# После установки: python -m spacy download en_core_web_sm

# Optional — только для zero-shot авторазметки через BART (шаг 3, рекомендуется)
# torch>=2.0
# transformers>=4.35
# Без этих пакетов AnnotationAgent использует rule-based fallback

# Optional — только для audio (modality=audio)
# openai-whisper>=20231117

# Optional — только для image (modality=image)
# ultralytics>=8.0

# Optional — Kaggle датасеты
# kaggle>=1.5
